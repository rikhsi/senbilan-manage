# Production — placeholders only. Real secrets are injected in CI/CD, never commit them.
APP_NAME=Senbilan Manage
DEFAULT_LOCALE=ru
API_BASE_URL=/api
SENTRY_ENABLED=true
SENTRY_DSN=
SENTRY_ENVIRONMENT=production
SENTRY_TRACES_SAMPLE_RATE=0.1
FEATURE_MOCK_API=false
FEATURE_PWA=true
NODE_ENV=production
