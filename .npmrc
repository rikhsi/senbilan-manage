# npm 10 arborist crashes ("edgesOut of null") while resolving the optional peer graph of
# @angular/build + vitest + storybook. Resolution is already locked in package-lock.json;
# this flag only affects `npm install <new-package>`. Re-evaluate when upgrading npm.
legacy-peer-deps=true
engine-strict=false
fund=false
audit=false
