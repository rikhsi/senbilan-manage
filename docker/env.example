# Copy to the deploy directory on the server as `.env` (next to docker-compose.yml).
# Compose reads this file for substitution. Do not commit the real file.

WEB_PORT=8080
IMAGE_TAG=local
CI_REGISTRY_IMAGE=senbilan-manage

API_BASE_URL=https://api.senbilan.uz
SENTRY_ENABLED=false
SENTRY_DSN=
SENTRY_ENVIRONMENT=production
