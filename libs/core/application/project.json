{
  "name": "core-application",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/core/application/src",
  "projectType": "library",
  "tags": ["layer:core", "kind:application"],
  "targets": {
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "{projectRoot}",
        "commands": ["tsc -b tsconfig.json --noEmit"],
        "parallel": false
      }
    },
    "lint-styles": {}
  }
}
