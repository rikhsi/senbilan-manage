export type AvatarSize = 'xs' | 'sm' | 'md' | 'lg' | 'xl';
export type AvatarRing = 'none' | 'primary' | 'secondary';

/** Distinct tint slots for initials fallback (CSS `data-tint` 0..n-1). */
export const AVATAR_TINT_COUNT = 5;
