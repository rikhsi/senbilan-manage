#!/usr/bin/env node
/**
 * Placeholder until feature i18n scopes land (PHASE 5).
 * Exit 0 so `npm run verify` stays usable during bootstrap.
 */
console.log('i18n:check — skipped (no scopes yet)');
process.exit(0);
